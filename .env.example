## ZAP Automation Framework configuration (local usage)
# Target under test
ZAP_TARGET=https://www.saucedemo.com

# Authentication mode: manual|form|json|script|http|autodetect|browser|client
ZAP_AUTH_METHOD=form
ZAP_USERNAME=standard_user
ZAP_PASSWORD=secret_sauce

# Form-based auth settings
ZAP_LOGIN_PAGE_URL=https://www.saucedemo.com/
ZAP_LOGIN_REQUEST_URL=https://www.saucedemo.com/
# Body template with placeholders: {username} {password} {token}
ZAP_LOGIN_REQUEST_BODY=username={username}&password={password}

# Optional regex helpers
# Extract CSRF token from login page (first capture group)
ZAP_TOKEN_REGEX=name=\"authenticity_token\" value=\"([^\"]+)\"
# Indicator in the authenticated landing page
ZAP_LOGGED_IN_INDICATOR_REGEX=Logout|Sign\ out

# Regexes to detect session state
ZAP_LOGGED_IN_REGEX=Logout|Sign\ out
ZAP_LOGGED_OUT_REGEX=Login|Sign\ in

# Script-based auth: path relative to repo (used when ZAP_AUTH_METHOD=script)
ZAP_AUTH_SCRIPT=scripts/security/zap-auth-scripts/form-auth.js
# Test Execution Framework Environment Variables

# SMTP Configuration for Email Notifications
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=true
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# Notification Settings
NOTIFICATION_FROM=noreply@yourcompany.com
SUMMARY_RECIPIENTS=team@yourcompany.com,manager@yourcompany.com
FAILURE_RECIPIENTS=dev-team@yourcompany.com,qa-team@yourcompany.com
CRITICAL_RECIPIENTS=oncall@yourcompany.com,tech-lead@yourcompany.com
NOTIFICATIONS_ENABLED=true

# Test Environment Configuration
TEST_ENVIRONMENT=development
BROWSER_NAME=chromium

# CI/CD Configuration
GITHUB_TOKEN=your-github-token
PWTEST_OUTPUT_DIR=test-results

# Performance Testing
# Base URL for k6 load tests; defaults to saucedemo if not set
BASE_URL=https://www.saucedemo.com
# Where k6 writes its JSON summary (optional override)
K6_SUMMARY_PATH=reports/load-tests/k6/summary.json

# Pact Broker configuration
PACT_BROKER_BASE_URL=
# Use either token OR username/password
PACT_BROKER_TOKEN=
PACT_BROKER_USERNAME=
PACT_BROKER_PASSWORD=

# Contract testing defaults
APPLICATION_NAME=EcommerceWebApp
PROVIDER_BASE_URL=http://localhost:8081
DEPLOY_ENVIRONMENT=test
APPLICATION_INSTANCE=

# End ZAP